// 2️⃣ Verificar Número Par
// Crie uma função chamada isEven que recebe um número como parâmetro e retorna true se o número for par e false se for ímpar.
function isEven(number) {
  return number % 2 == 0;
}
console.log(isEven(2));
