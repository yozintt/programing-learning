// 3️⃣ Calcular o Quadrado
// Crie uma função chamada square que recebe um número como parâmetro e retorna o quadrado desse número.

function Square(number) {
  return number * number;
}
console.log(Square(5));
