// 1️⃣ Dobrar Número
// Crie uma função chamada dobrar que recebe um número como parâmetro e retorna o dobro desse número.
function double(number) {
  let numberDouble = number * 2;
  return numberDouble;
}
console.log(double(2));
