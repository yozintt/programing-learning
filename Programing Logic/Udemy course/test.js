// Gerador de numeros aleatorios com Math.random(), estabelecendo limite entre 0 e 1 em numeros menores que 0,97, mostrando a quantidade inicial ate a final e tambem quantas vezes no total, atraves do While loop.
/*
let quantidade = "";
while (Math.random() < 0.97) {
  console.log(quantidade++);
}
console.log("Qtd:", quantidade);

for (let i = 1; i <= 3; i++) {
  let linha = "";
  for (let j = 1; j <= 3; j++) {
    linha += `[${i}, ${j}]`;
  }
  console.log(linha);
}

*/

let linha = "";

for (let i = "#"; i != "#######"; i += "#") {
  console.log(i);
}
